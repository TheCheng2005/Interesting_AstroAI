# ── Stage 0: dataset construction (dataset/pipeline.py) ────────────────────
# Queries MAST and the Hubble Source Catalog, then cuts and encodes 223,195
# thumbnails. Needs CasJobs credentials in a .env file.
mastcasjobs       # HSC v3 via the CasJobs SQL service
python-dotenv     # reads CASJOBS_WSID / CASJOBS_PW from .env
scipy             # cKDTree for the 10" deduplication
astropy           # WCS, Cutout2D, ZScaleInterval
astroquery        # MAST product inventory and downloads
pyarrow           # the intermediate and final Parquet catalogues
pandas
numpy
h5py              # the cutout archive
Pillow            # JPEG encoding of each cutout

# ── Stage 1: scoring (scoring/*.py) ────────────────────────────────────────
# Needs the stage-0 output and an API key.
google-genai      # Gemini 3.1 Flash-Lite
openai            # Qwen3.5-397B-A17B, via OpenRouter's OpenAI-compatible API
pydantic          # the structured-output schemas the models fill in
tqdm

# ── Stage 2: catalogue cross-match (unidentified_objects/) ─────────────────
# Public services only, no key required.
pyvo              # SIMBAD and NED TAP

# ── Stage 2b: released candidate catalogue ─────────────────────────────────
# Needs an ADS token at ~/.ads_api_key and GEMINI_API_KEY. The O'Ryan
# screening step also reads local catalogue CSVs (ORYAN_CATALOGUE_DIR).
requests          # the ADS API

# ── Stage 3: metrics (metrics/*.py) ────────────────────────────────────────
# Standard library only. Nothing to install.
